# dropcrate-web
Dropcrate is a storage for all of your files

## Local dependencies
Before running the application, start local infrastructure:

```bash
docker compose -f docker/compose.yaml up -d
```
